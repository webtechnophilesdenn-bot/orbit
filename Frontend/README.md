# Orbit — Frontend

React + Vite + Tailwind CSS frontend for Orbit.

## Setup

```bash
npm install
npm run dev       # starts on http://localhost:5173
npm run build     # production build
npm run preview   # preview production build
npm run lint      # run ESLint
```
