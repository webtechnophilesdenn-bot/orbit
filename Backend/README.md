# Orbit — Backend

Express API server for Orbit with PostgreSQL.

## Setup

```bash
cp .env.example .env   # edit with your database credentials
npm install
npm run dev            # starts on http://localhost:5000 (with --watch)
npm start              # production start
```
